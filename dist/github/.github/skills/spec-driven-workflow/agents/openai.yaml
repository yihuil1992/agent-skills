interface:
  display_name: "Spec-Driven Workflow"
  short_description: "Clarify, spec, self-review, run, remember"
  default_prompt: "Use $spec-driven-workflow in strict mode: complete the Brainstorm Gate, converge a spec, implement it, verify it, pass the Verification And Memory Gate, and update local project memory."
